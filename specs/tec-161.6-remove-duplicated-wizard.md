# TEC-161.6: Remove Duplicated Wizard in asigurare/page.tsx

**Assignee:** Frontend Lead
**Priority:** P1
**Estimate:** N/A — **implemented by CTO in this heartbeat**
**Dependencies:** None

## CTO Note (2026-05-18)

**Implemented in this heartbeat.** The asigurare page was reduced from 100 lines of duplicated wizard logic to 21 lines:

Before: Custom `WizardContent` with duplicate `renderStep()` switch, duplicate stepper construction, duplicate progress indicator, hardcoded RO/EN step labels.
After: Imports `Wizard` from `@blaj/ui`, wraps it in the page-specific header and card container.

The shared `Wizard` component in `packages/ui/src/components/wizard.tsx` now handles all step rendering, stepper, and progress — the page only provides the outer layout.

## What to Verify

- [ ] Wizard renders correctly at `/ro/asigurare` and `/en/asigurare`
- [ ] Stepper shows correct step names (from i18n translations, not hardcoded)
- [ ] Progress indicator shows correct step count
- [ ] No visual regression from the previous custom animation (previously used x-axis animation, now uses slideUpFade from shared Wizard)
